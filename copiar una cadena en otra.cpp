#include<stdio.h>
#include<conio.h>
#include<string.h>
#include<stdlib.h>
using namespace std;
int main (void){
	int i;
	char name[50];
	char ape[50];
	char nape[160];
	cout<<"Introduce tu nombre";
	cin.getline(name,50);
	cout<<"Introduce tu apellido";
	cin.getline(ape,50);
	strcpy(nape,name); //Esta instruccion permite copiar una cadena de A en B
	strcpy(nape,ape);
	i=strlen(B);
	C[i]=' ';
	cout>>name[i];

	
	getch();
	return 0;
}

